/*
Primitive Types--
Integers: u8, i8, u16, i16, u32, i32, u64, i64, u128, i128 (number of bits they take in memory)
Floats: f32, f64
Boolean (bool)
Characters (char)
Tuples
Arrays 
*/

pub fn run() {

  println!("MAX i32 is {max32}", max32 = std::i32::MAX);
  println!("MAX i64 is {max64}", max64 = std::i64::MAX);
}